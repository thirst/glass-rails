<article>
  <figure>
    <img src="<%= @content.image_url %>">
  </figure>
  <section {{section_options}}>
    <table {{table_options}}> 
      {{table_header}}
      {{table_rows}}
    </table>
  </section>
  {{footer}}
</article>
